#pragma once

#include <SFML/Audio/Music.hpp>
#include <filesystem>
#include <string>
namespace Threads {
    extern std::string userInput;
    extern bool running;
    extern bool readyForInput;
} // namespace Threads

// inline sf::Music music{};
// inline std::filesystem::path musicDir{};
